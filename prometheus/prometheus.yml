global:
  scrape_interval: 60s    # reduce scrape frequency to lower RAM/CPU usage

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["prometheus:9090"]

  - job_name: node
    static_configs:
      - targets: ["node-exporter:9100"]
